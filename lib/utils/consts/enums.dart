// --- Template ---

// List of Enums
// They cannot be created inside class.

enum TextSizes { small, medium, large, veryLarge }

enum Status { loading, completed, error, none }

enum PaymentMethods {
  paypal,
  aba,
  aceleda,
  visa,
  masterCard,
  applePay,
  googlePay,
  creditCard
}

enum ConnectivityType {
  mobile,
  wifi,
  ethernet,
  vpn,
  bluetooth,
  other,
  none,
}
